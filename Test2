SELECT
    ra.fabid,
    ra.jobid,
    ra.jobver,
    LISTAGG(ra.chart_type || ',' || ra.ruleid, ',') WITHIN GROUP (ORDER BY ra.chart_type) AS combined_column
FROM
    rule_action ra
GROUP BY
    ra.fabid,
    ra.jobid,
    ra.jobver;
了解！我來幫你設計一個簡約又好看的 ASP.NET WebForms 專案畫面，並搭配 DevExpress 19.2 Data Grid（ASPxGridView）來處理資料列的新增、編輯、刪除與查詢，後端使用 Oracle.DataAccess 連線 Oracle。下面是完整範例，分別包含 .aspx、.aspx.cs 和 CSS。

預期畫面
	•	上方 Tab（使用 DevExpress 的 ASPxPageControl）
	•	中間有「新增新值」按鈕
	•	下方為 ASPxGridView 表格，具備篩選、編輯、刪除、新增
	•	彈出編輯視窗（PopupControl）

1. ASPX 頁面（Default.aspx）

<%@ Page Language="C#" AutoEventWireup="true" CodeFile="Default.aspx.cs" Inherits="Default" %>
<%@ Register Assembly="DevExpress.Web.v19.2" Namespace="DevExpress.Web" TagPrefix="dx" %>

<!DOCTYPE html>
<html>
<head runat="server">
    <title>Fab Eqp 管理系統</title>
    <link href="Style.css" rel="stylesheet" />
</head>
<body>
    <form id="form1" runat="server">
        <div class="container">

            <!-- Tab 控制 -->
            <dx:ASPxPageControl ID="TabControl" runat="server" Width="100%">
                <TabPages>
                    <dx:TabPage Text="Tab1" />
                    <dx:TabPage Text="Tab2" />
                </TabPages>
            </dx:ASPxPageControl>

            <!-- 新增新值按鈕 -->
            <div class="button-bar">
                <dx:ASPxButton ID="btnAddNew" runat="server" Text="新增新值" CssClass="btn-add"
                    AutoPostBack="false" OnClick="btnAddNew_Click">
                </dx:ASPxButton>
            </div>

            <!-- Grid -->
            <dx:ASPxGridView ID="GridView" runat="server" AutoGenerateColumns="False" KeyFieldName="ID"
                Width="100%" CssClass="grid" OnRowUpdating="GridView_RowUpdating"
                OnRowDeleting="GridView_RowDeleting" OnRowInserting="GridView_RowInserting"
                OnCustomCallback="GridView_CustomCallback">
                <SettingsEditing Mode="PopupEditForm" PopupEditFormHorizontalAlign="WindowCenter" PopupEditFormVerticalAlign="WindowCenter" />
                <SettingsBehavior AllowFocusedRow="True" />
                <SettingsPager PageSize="10" />
                <Columns>
                    <dx:GridViewCommandColumn ShowEditButton="true" ShowDeleteButton="true" ShowNewButtonInHeader="true" />
                    <dx:GridViewDataTextColumn FieldName="FAB" Caption="Fab" />
                    <dx:GridViewDataTextColumn FieldName="EQP" Caption="Eqp" />
                    <dx:GridViewDataTextColumn FieldName="CHP" Caption="Chp" />
                    <dx:GridViewDataTextColumn FieldName="JOBS" Caption="Jobs" />
                </Columns>
            </dx:ASPxGridView>

        </div>
    </form>
</body>
</html>

2. 後端程式碼（Default.aspx.cs）

using System;
using System.Data;
using Oracle.DataAccess.Client; // Oracle.DataAccess 用於 Oracle 連線

public partial class Default : System.Web.UI.Page
{
    private string connectionString = "User Id=your_user;Password=your_password;Data Source=your_datasource";

    protected void Page_Load(object sender, EventArgs e)
    {
        if (!IsPostBack)
        {
            LoadGridData();
        }
    }

    private void LoadGridData()
    {
        DataTable dt = new DataTable();
        using (OracleConnection conn = new OracleConnection(connectionString))
        {
            string query = "SELECT ID, FAB, EQP, CHP, JOBS FROM YOUR_TABLE";
            using (OracleDataAdapter da = new OracleDataAdapter(query, conn))
            {
                da.Fill(dt);
            }
        }

        GridView.DataSource = dt;
        GridView.DataBind();
    }

    protected void GridView_RowInserting(object sender, DevExpress.Web.Data.ASPxDataInsertingEventArgs e)
    {
        string fab = e.NewValues["FAB"]?.ToString();
        string eqp = e.NewValues["EQP"]?.ToString();

        if (string.IsNullOrEmpty(eqp) || eqp.Length > 5)
        {
            throw new Exception("EQP 欄位必須為 5 個字元以內！");
        }

        using (OracleConnection conn = new OracleConnection(connectionString))
        {
            conn.Open();
            string insertSql = "INSERT INTO YOUR_TABLE (ID, FAB, EQP, CHP, JOBS) VALUES (YOUR_SEQ.NEXTVAL, :FAB, :EQP, :CHP, :JOBS)";
            using (OracleCommand cmd = new OracleCommand(insertSql, conn))
            {
                cmd.Parameters.Add(":FAB", fab);
                cmd.Parameters.Add(":EQP", eqp);
                cmd.Parameters.Add(":CHP", e.NewValues["CHP"]);
                cmd.Parameters.Add(":JOBS", e.NewValues["JOBS"]);
                cmd.ExecuteNonQuery();
            }
        }

        e.Cancel = true;
        GridView.CancelEdit();
        LoadGridData();
    }

    protected void GridView_RowUpdating(object sender, DevExpress.Web.Data.ASPxDataUpdatingEventArgs e)
    {
        string eqp = e.NewValues["EQP"]?.ToString();

        if (string.IsNullOrEmpty(eqp) || eqp.Length > 5)
        {
            throw new Exception("EQP 欄位必須為 5 個字元以內！");
        }

        using (OracleConnection conn = new OracleConnection(connectionString))
        {
            conn.Open();
            string updateSql = "UPDATE YOUR_TABLE SET FAB = :FAB, EQP = :EQP, CHP = :CHP, JOBS = :JOBS WHERE ID = :ID";
            using (OracleCommand cmd = new OracleCommand(updateSql, conn))
            {
                cmd.Parameters.Add(":FAB", e.NewValues["FAB"]);
                cmd.Parameters.Add(":EQP", eqp);
                cmd.Parameters.Add(":CHP", e.NewValues["CHP"]);
                cmd.Parameters.Add(":JOBS", e.NewValues["JOBS"]);
                cmd.Parameters.Add(":ID", e.Keys["ID"]);
                cmd.ExecuteNonQuery();
            }
        }

        e.Cancel = true;
        GridView.CancelEdit();
        LoadGridData();
    }

    protected void GridView_RowDeleting(object sender, DevExpress.Web.Data.ASPxDataDeletingEventArgs e)
    {
        string userId = Session["ID"]?.ToString();

        if (!UserHasDeletePermission(userId))
        {
            throw new Exception("您沒有刪除權限！");
        }

        using (OracleConnection conn = new OracleConnection(connectionString))
        {
            conn.Open();
            string deleteSql = "DELETE FROM YOUR_TABLE WHERE ID = :ID";
            using (OracleCommand cmd = new OracleCommand(deleteSql, conn))
            {
                cmd.Parameters.Add(":ID", e.Keys["ID"]);
                cmd.ExecuteNonQuery();
            }
        }

        e.Cancel = true;
        LoadGridData();
    }

    private bool UserHasDeletePermission(string userId)
    {
        if (string.IsNullOrEmpty(userId)) return false;

        using (OracleConnection conn = new OracleConnection(connectionString))
        {
            conn.Open();
            string sql = "SELECT COUNT(1) FROM USER_AUTH WHERE USER_ID = :USER_ID";
            using (OracleCommand cmd = new OracleCommand(sql, conn))
            {
                cmd.Parameters.Add(":USER_ID", userId);
                object result = cmd.ExecuteScalar();
                return Convert.ToInt32(result) > 0;
            }
        }
    }

    protected void btnAddNew_Click(object sender, EventArgs e)
    {
        GridView.AddNewRow();
    }

    protected void GridView_CustomCallback(object sender, DevExpress.Web.ASPxGridViewCustomCallbackEventArgs e)
    {
        LoadGridData();
    }
}

3. CSS 設計（Style.css）

body {
    font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f5f7fa;
    margin: 0;
}

.container {
    margin: 20px auto;
    width: 90%;
}

.button-bar {
    margin: 10px 0;
    text-align: right;
}

.btn-add {
    background-color: #007acc;
    color: white;
    border-radius: 4px;
    padding: 6px 12px;
    border: none;
    cursor: pointer;
}

.btn-add:hover {
    background-color: #005fa3;
}

.grid {
    margin-top: 10px;
}

.dxgvControl_PlasticBlue {
    background-color: #ffffff !important;
}

.dxgvHeader_PlasticBlue {
    background-color: #007acc !important;
    color: #ffffff !important;
    font-weight: bold;
}

.dxgvDataRow_PlasticBlue {
    background-color: #f9f9f9;
}

.dxgvFocusedRow_PlasticBlue {
    background-color: #e6f7ff !important;
}

核心功能
	•	DevExpress 19.2 控制項：ASPxPageControl、ASPxButton、ASPxGridView
	•	Oracle 資料連接：Oracle.DataAccess（ODP.NET）
	•	權限檢查：刪除及編輯時根據 session["ID"] 驗證權限
	•	簡約 UI：CSS 增強畫面一致性與美感
	•	彈性增刪修查：GridView + PopupEditForm

如果你有更具體的需求（例如彈出窗客製、資料驗證細節、Ajax 無刷新等），可以再調整！
有需要完整專案架構或 DevExpress 元件安裝說明，我也可以補上！